ALTER TABLE `deliverytrucks`
	ADD COLUMN IF NOT EXISTS `MaxCapacity` VARCHAR(50) NULL DEFAULT NULL AFTER `CBM`,
	ADD COLUMN IF NOT EXISTS `YearAndModel` VARCHAR(50) NULL DEFAULT NULL AFTER `MaxCapacity`;
